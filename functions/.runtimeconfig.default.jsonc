/**
 * Create a .runtimeconfig.json to set local environment variables for development

 * Before deployment, set each configuration value with `firebase functions:config:set <service>.<key>="<value>" <service>.<key>="<value>"`.
 */

{
  "env": {
    "bot_token": "token",
    "meetwhen_api": "https://meetwhen.com/api/graphql"
  }
}
